import Metrics from './Metrics'

const AppStyles = {
    screen: {
        centering: {
            alignItems: 'center'
        },
        section: {
            margin: 10,
            padding: 10
        }
    }
}

export default AppStyles
